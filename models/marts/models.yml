version: 2

models:
  - name: time_spine_daily
    description: "A time spine with one row per day, from ~5 years back to 30 days forward."
    time_spine:
      standard_granularity_column: date_day

    columns:
      - name: date_day
        description: "The base date column for daily granularity"
        granularity: day
