version: 2

sources:
  - name: raw
    schema: raw                # schema where the raw table lives
    tables:
      - name: orders
        loaded_at_field: loaded_at   # column used to check freshness
        freshness:
          warn_after: {count: 1, period: hour}
          error_after: {count: 2, period: hour}